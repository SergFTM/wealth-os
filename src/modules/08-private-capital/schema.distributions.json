{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PCDistribution",
  "type": "object",
  "required": ["id", "fundId", "commitmentId", "entityId", "date", "amount", "currency", "type", "status"],
  "properties": {
    "id": { "type": "string" },
    "fundId": { "type": "string" },
    "commitmentId": { "type": "string" },
    "entityId": { "type": "string" },
    "date": { "type": "string", "format": "date" },
    "amount": { "type": "number" },
    "currency": { "type": "string", "default": "USD" },
    "type": { 
      "type": "string",
      "enum": ["cash", "in_kind", "return_capital", "fee"]
    },
    "status": { 
      "type": "string",
      "enum": ["planned", "received", "pending"]
    },
    "receivedDate": { "type": ["string", "null"], "format": "date" },
    "docIds": { "type": "array", "items": { "type": "string" } },
    "sourceRef": { "type": ["string", "null"] },
    "notes": { "type": ["string", "null"] },
    "createdAt": { "type": "string", "format": "date-time" },
    "updatedAt": { "type": "string", "format": "date-time" }
  }
}
